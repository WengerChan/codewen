[package]
name = "codewen-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Codewen workspace dependency.
# Add new Codewen surface area to `codewen-core-api` instead of depending on
# additional `codewen-*` crates here.
codewen-core-api = { workspace = true }
tracing = { workspace = true }
