load("//:defs.bzl", "codewen_rust_crate")

codewen_rust_crate(
    name = "exec",
    crate_name = "codewen_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
