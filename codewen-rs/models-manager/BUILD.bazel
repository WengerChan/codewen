load("//:defs.bzl", "codewen_rust_crate")

codewen_rust_crate(
    name = "models-manager",
    compile_data = [
        "models.json",
        "prompt.md",
    ],
    crate_name = "codewen_models_manager",
)
