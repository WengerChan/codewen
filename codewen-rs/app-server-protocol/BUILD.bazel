load("//:defs.bzl", "codewen_rust_crate")

codewen_rust_crate(
    name = "app-server-protocol",
    crate_name = "codewen_app_server_protocol",
    test_data_extra = glob(
        ["schema/**"],
        allow_empty = True,
    ),
)
